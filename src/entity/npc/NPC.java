package entity.npc;

import level.Level;
import entity.Entity;

/**
 *
 * @author Creatorri
 */
public class NPC extends Entity {

    public NPC(Level l) {
        super(l, Level.NUMLEVELS);
    }

    @Override
    public void update() {
    }
}
